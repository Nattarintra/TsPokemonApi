import { radii } from "@theme/tokens/radii";

export const shape = {
    borderRadius: radii.sm,

    radius: {
        none: radii.none,
        small: radii.sm,
        medium: radii.md,
        large: radii.lg,
        full: radii.full,
    },
};